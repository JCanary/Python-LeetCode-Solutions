// 4ms Runtime Beats 80.72%
// 17.84MB Memory Beats 39.34%
class Solution:
    def isPalindrome(self, x: int) -> bool:
        txt = str(x)[::-1]
        if str(txt) == str(x):
            x = True
        else:
            x = False
        return x
